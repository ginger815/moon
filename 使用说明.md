# 中秋祝福页 · 使用说明

原项目：https://github.com/kldby666/moon （单文件 `index.html` + `music.mp3`）

## 一、本次改动

### 1. 删除顶部滚动弹幕
移除了页面顶部滚动的「🌕 花好月圆 ❤️ 阖家团圆 …」跑马灯，同时清理了它专用的
`@keyframes scroll` / `.animate-scroll` 样式，不留无用代码。
顶部静态标题「中秋快乐 / 2026 · 万事顺遂」保留。

### 2. 初始状态关闭自动模式
- `autoFireworks` 初始值 `true` → `false`，打开页面不再自动持续放烟花。
- 去掉了加载时给「自动模式」按钮加激活态的 `classList.add('active')`，改为调用
  `updateButtonStates()` 同步按钮状态，按钮初始显示为未激活。
- 自动功能本身没删：点右下角「自动模式」按钮或按空格键仍可随时开启；开场的 3 发欢迎烟花保留。

### 3. 点击灯笼 → 输入名字 → 中央显示专属祝福
- 点击任意一个灯笼（右上「福」/ 左上「禄」），弹出居中的金色灯笼风格弹窗：
  **「输入你的名字领取专属祝福」**，内含输入框 + 「领取祝福」「再想想」按钮。
- 输入名字后（点按钮或按回车），弹窗关闭，屏幕正中黑色区域以描金渐变毛笔字体显示
  **「名字，随机祝福语」**，例如：`林清和，愿你花好月圆，事事圆满`。
- 祝福语从 10 条预置祝福里随机抽取，且连续两次不会重复。
- 名字部分字号更大、单独高亮；文字带金色渐变 + 外发光 + 入场缩放淡入动画。
- 配套细节：
  - 名字为空时输入框抖动提示，不会提交；
  - 名字用 `textContent` 纯文本渲染，不会被当作 HTML 注入；
  - 输入框里按空格不会误触发「空格切换自动模式」的快捷键；
  - 弹窗内点击不会误放烟花（已拦截）；
  - 点遮罩或按 Esc 可关闭弹窗，点灯笼可重新领取、换一个祝福；
  - 名字记在 `localStorage`，再次领取时自动填好；
  - 领取成功时中央会放 3 发烟花（中间一发是爱心）庆祝。

### 4. 顺带做的「变成网址」相关处理
- 原来依赖外部资源，现在全部本地化，断网也能正常打开：
  | 原来 | 现在 |
  | --- | --- |
  | `https://cdn.tailwindcss.com` | 本地 `tailwind.js` |
  | `http://kldby666.github.io/moon/music.mp3` | 本地 `music.mp3` |
  | `https://img0v0.6521520.xyz/img/yutu.png` | 本地 `yutu.png` |
- 新增零依赖静态服务器 `server.js`（支持 Range 请求，音频可拖动进度）和双击启动的 `start.cmd`。

## 二、目录结构

```
moon/
├── index.html     改造后的页面（核心文件）
├── tailwind.js    本地化的 Tailwind（原 CDN）
├── music.mp3      背景音乐
├── yutu.png       玉兔图片
├── server.js      零依赖静态服务器
└── start.cmd      双击启动并自动打开浏览器
```

## 三、如何访问（网址形式）

**在线地址（已部署，推荐分享）：<https://ginger815.github.io/moon/>**
仓库：<https://github.com/ginger815/moon>

**本地访问：**

**方式一（推荐，最简单）**：双击 `start.cmd`，浏览器自动打开 <http://127.0.0.1:8099/>。

**方式二（命令行）**：

```powershell
cd C:\Users\10138\Documents\dsh\moon
node server.js 8099
# 然后浏览器打开 http://127.0.0.1:8099/
```

**方式三（手机/局域网访问）**：让局域网内设备也能打开

```powershell
$env:MOON_HOST="0.0.0.0"; node server.js 8099
# 手机浏览器访问 http://<这台电脑的局域网IP>:8099/
```

**方式四（直接双击 `index.html`）**：也能看，但 `file://` 下背景音乐播放、`localStorage`
记忆名字会受限，建议用上面的服务器方式。

**已经发布到 GitHub Pages**：本目录内容即为线上内容，改动后重新发布只需一条命令
（脚本会自动增量提交并刷新 Pages，无需 git push，也无需本地配置 git 凭据）：

```powershell
cd C:\Users\10138\Documents\dsh\moon
$env:GH_TOKEN = (gh auth token); node deploy.js ginger815 moon public; $env:GH_TOKEN = $null
```

发布后约 1 分钟访问 <https://ginger815.github.io/moon/> 即为最新版本。
也可以换成任意其他静态托管（Vercel / Netlify / 对象存储 + CDN），本项目零构建、纯静态。

## 四、自动化验收

改动后跑了 34 项 jsdom 自动验收（顶部弹幕移除、自动模式初始关闭、弹窗与祝福全流程、
XSS 与快捷键等边界情况），全部通过；线上地址另外做了 8 项验收，并与本地 `index.html`
做了 sha256 比对，内容完全一致：

```powershell
cd C:\Users\10138\Documents\dsh
node _verifytest\run-tests.js        # 34 项本地功能验收
node _verifytest\http-check.js       # 8 项本地 HTTP 验收
$env:GH_TOKEN = (gh auth token); node _verifytest\verify-pages.js; $env:GH_TOKEN = $null   # 线上验收
```

`_verifytest` 只是测试脚手架，不影响站点，可随时删除。
